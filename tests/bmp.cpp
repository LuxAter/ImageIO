#include <catch2/catch.hpp>

#include "iio/bmp.hpp"

#define IMG_SIZE 100

// TEST_CASE("bmp") {
  // SECTION("grayscale") {
    // std::uint8_t grayscale_data[IMG_SIZE][IMG_SIZE];
    // for (std::size_t y = 0; y < IMG_SIZE; ++y) {
      // std::uint8_t color = static_cast<std::uint8_t>(
          // static_cast<float>(y) / static_cast<float>(IMG_SIZE) * 255);
      // for (std::size_t x = 0; x < IMG_SIZE; ++x) {
        // grayscale_data[y][x] = color;
      // }
    // }
    // iio::write_bmp("test.bmp", IMG_SIZE, IMG_SIZE, 1, &grayscale_data[0][0]);
  // }
// }
